// Names: Store the names of a few of your friends in an array called names.
// Print each person’s name by accessing each element in the list, 
// one at a time.
let names = ["Jack", "Judy", "Shah", "Ali"];
// printing names without for loop
console.log(names[0]);
console.log(names[1]);
console.log(names[2]);
console.log(names[3]);
// printing names with for loop
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}
export {};
