/* Question 21: Think of something you could store in a TypeScript Object. Write a program that 
creates Objects containing these items. */

const booksDetails = {
    name : 'David Copperfield',
    writer : 'Charles Dickens',
    published : '1849',
    price : '$9',
    };

console.log (`The ${booksDetails.name} is written by ${booksDetails.writer} and was published in ${booksDetails.published}.\nPrice of the book is ${booksDetails.price}.`);
